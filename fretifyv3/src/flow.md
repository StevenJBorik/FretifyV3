key of song -- > get relative minor/major --> create mapping between drawScale Root note and major and minor keys. --> pass keys to UI -> Create UI options for song section frequency (2x = midpoint between each section, trigger scale change), Ascending/Descending Scale order, Number of frets to display per switch, , Fretboard Range, String range, -- > Web Audio API for lighting up scale note --> UI development 